//Function Types

//Named Function
function soma1 (x, y) {
  return x + y
}

//Anonymous Function
var soma2 = function (x, y) {
  return x + y
}

//ES6 - Arrow Function
const soma3 = (x, y) => {
  return x + y
}

const soma4 = (x, y) => x + y

const array = [1, 2, 3, 4, 5].map(x => x * 100)